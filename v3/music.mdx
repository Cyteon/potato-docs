---
title: "Music"
description: "music system"
---

Run `/music play <query>` while in a voice channel, so it can join you. 

Run `/music volume <volume>` to change the volume of the music.

Run `/disconnect` to make the bot stop playing the music.

use `/pause` for it to pause

use `/resume` for it to resume